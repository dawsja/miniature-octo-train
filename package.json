{
  "name": "manage-video-download-links",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/server.ts",
    "start": "bun run src/server.ts"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "bun-types": "^1.3.4"
  }
}
